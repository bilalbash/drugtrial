<% if current_user %>

<% else %>
	<div class="jumbotron masthead">
	  <div class="container">
	    <h1>Drug Trial</h1>
	    <p>patient recovery tool</p>
	    <p>
	      <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-primary btn-large" %>
	    </p>
	  </div>
	</div>
<% end %>